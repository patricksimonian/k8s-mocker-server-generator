# Kubernetes Mock API Server

This is a mock Kubernetes API server generated by the Kubernetes Mock Server Generator. It simulates a real Kubernetes API server and is compatible with kubectl.

## Features

- Fully compatible with kubectl
- Supports all standard Kubernetes API operations (GET, POST, PATCH, DELETE, WATCH)
- Supports subresources (/status, /scale, etc.)
- Configurable storage backend (in-memory, file, Firestore)
- Configurable authentication
- Configurable logging

## Getting Started

### Prerequisites

- Node.js 14+
- npm or yarn

### Installation

```bash
npm install
