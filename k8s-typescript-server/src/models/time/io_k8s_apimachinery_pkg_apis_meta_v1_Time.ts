/**
* Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
* @resourceType time
* @kind Time
*/
export interface io_k8s_apimachinery_pkg_apis_meta_v1_Time {
}

/**
* Create a new Time with default values
* @param data - Optional partial data to override defaults
* @returns A new io_k8s_apimachinery_pkg_apis_meta_v1_Time instance with defaults applied
*/
export function createio_k8s_apimachinery_pkg_apis_meta_v1_Time(data?: Partial<io_k8s_apimachinery_pkg_apis_meta_v1_Time>): io_k8s_apimachinery_pkg_apis_meta_v1_Time {
 return {
 };
}
